# App icon

`LNS.icns` contains the unchanged PNG from
`crates/lns-service/assets/lnsTemplate@2x.png` in an `icp5` (32 × 32) entry.
The bundle also copies that original asset for its template menu-bar icon.
